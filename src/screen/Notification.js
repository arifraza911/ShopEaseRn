import { Alert, StyleSheet, Text, View } from 'react-native'
import React, { useEffect } from 'react'


const Notification = () => {
   useEffect(()=>{
    alert("This section is empty right now...")
   },[])
  return (
    <View>
      
    </View>
  )
}

export default Notification

const styles = StyleSheet.create({})